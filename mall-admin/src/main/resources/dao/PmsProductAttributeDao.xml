<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.dao.PmsProductAttributeDao">
    <select id="getProductAttrInfo" resultType="com.macro.mall.dto.ProductAttrInfo">
        SELECT DISTINCT
            pav.product_attribute_id AS attributeId
        FROM
            pms_product_attribute_value pav
        WHERE
            pav.product_id IN (
                SELECT id FROM pms_product WHERE product_category_id = #{id}
            )
    </select>
</mapper>
